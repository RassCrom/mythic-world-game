{
  "name": "unstable-dragons-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/engine.test.js src/index.test.js",
    "fingerprint": "node -e \"import('./src/index.js').then(m=>console.log(JSON.stringify(m.deckFingerprint())))\""
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
